import React from 'react'

import s from '@/src/entities/profile/settings/myPayment/ui/myPayment.module.scss'

type Props = {}
export const MyPayment = ({}: Props) => {
  return (
    <div className={s.profile}>
      <div className={s.content}>MyPayment</div>
    </div>
  )
}
